language: node_js
node_js:
    - "0.10"
before_install:
    - "export DISPLAY=:99.0"
    - "sh -e /etc/init.d/xvfb start"
install:
    - npm install
script:
    - make all
    - make start-app
    - make acceptance-test
    - make stop-app
    - make docs
    - make coverage
after_script:
    - make publish-coverage

